fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
# <2: the base image's pycuda and the torch 2.3.1 cu121 wheels are compiled
# against the NumPy 1.x C ABI — NumPy 2 makes them fail at import.
numpy>=1.26.0,<2
opencv-python-headless>=4.10.0
# PyWorker framework (worker.py) — NOT `vastai` (that's the CLI, whose classes
# have a different, incompatible API). Same package the video-preprocess worker uses.
vastai-sdk
